language: node_js
node_js:
  - 0.12
  - 4
  - 6
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-6
env:
  - CXX=g++-6
script:
  - ( while true; do echo keep alive!; sleep 60; done ) &
  - npm run travis-coverage-$TRAVIS_NODE_VERSION
