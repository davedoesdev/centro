environment:
  matrix:
    - nodejs_version: "10.5.0"

install:
  - ps: Update-NodeJsInstallation $env:nodejs_version
  - npm install

test_script:
  - node --version
  - npm --version
  - npm run test-win

build: off
